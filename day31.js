/*
*Author: Alexander Domakyaareh
*File: write a function which will convert a binary number into a decimal number
*/

const biToDec = (input) => {
    return parseInt(input, 2)
}


console.log(biToDec(10));
