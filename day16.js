/*
*Author: Alexander Domakyaareh
*File: function to reverse an array
*/

let array = ['alex', 'cat', 'zoro', 1];

const reverseArray = (input) => {
    return input.reverse();
}


console.log(`the reverse of [${array}] is {${reverseArray(array)}] `);

